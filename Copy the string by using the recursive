def copy_string(s):
    """Recursively copy the given string."""
    if s == "":
        return s
    else:
        return s[0] + copy_string(s[1:])

# Example usage
original_string = input("Enter the string to copy: ")
copied_string = copy_string(original_string)
print(f"The copied string is: {copied_string}")
